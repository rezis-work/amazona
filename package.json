{
  "name": "amazona",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && tsc --noEmit || true && cd ../frontend && npm install && npm run build",
    "start": "cross-env TG_ALLOW_MIXED=ALLOW node backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
